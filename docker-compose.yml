version: "3.8"
services:
  frontend:
    image: thesis-frontend-dev
    build: .
    ports:
      - 3000:3000
    environment:
      CHOKIDAR_USEPOLLING: "true"
    volumes:
      - ./:/home/node/app
      - react-modules:/home/node/app/node_modules
volumes: 
  react-modules:
